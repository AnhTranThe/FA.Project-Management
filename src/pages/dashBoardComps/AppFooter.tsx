

const AppFooter = () => {
  //   const { layoutConfig } = useContext(LayoutContext);
  return (
    <div className="layout-footer">
      <span className="font-medium ml-2">MockProject</span>
    </div>
  );
};

export default AppFooter;
